- Stands for secure shell
- A cryptographic network protocol to securely access, control and manage a computer or server remotely

## How to use?

- You will use the following command to access a remote server

```bash
ssh username@address
```

- You can use the flag -p to access a specific port.
- SSH is defaulted to port 22 but can be changed in a servers ssh settings