- Used to output all printable character sequences at least 4 characters long in a file
- Helps strip out all non human readable text from a file and only show human text

```bash
string data.txt | grep "search_term"
```